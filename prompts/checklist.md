# AI レビュー チェックリスト

> このチェックリストは段階的に拡充する。Phase パラメータで適用範囲を制御。

---

## Phase 1：致命 8 項目（導入直後〜2 週間）

| ID | チェック項目 | 観点 | 重大度 |
|----|------------|------|--------|
| S-1 | `allow read, write: if true` になっていないか | セキュリティ | 🔴致命 |
| S-2 | `request.auth != null` の検証が入っているか | セキュリティ | 🔴致命 |
| S-3 | 他人のドキュメントを読める構造になっていないか（userId スコープ） | セキュリティ | 🔴致命 |
| S-5 | `firestore.rules` の変更を必ず検知する | セキュリティ | 🔴致命 |
| K-1 | `apiKey`, `secret`, `password`, `token`, `credential` がハードコードされていないか | 秘密情報 | 🔴致命 |
| K-2 | `.env` ファイルが `.gitignore` に含まれているか | 秘密情報 | 🔴致命 |
| K-3 | `serviceAccount*.json`, `firebase-adminsdk-*.json` が `.gitignore` に含まれているか | 秘密情報 | 🔴致命 |
| B-8 | `useEffect` の依存配列が正しいか（無限ループ → Firestore 無限読込リスク） | バグ | 🔴致命 |

---

## Phase 2：+ 警告 11 項目（3〜4 週目に追加）

| ID | チェック項目 | 観点 | 重大度 | 備考 |
|----|------------|------|--------|------|
| S-4 | Cloud Functions でサーバー側の認証検証（verifyIdToken）が実装されているか | セキュリティ | 🟡警告 | Functions がある場合のみ |
| S-6 | Firestore ルールで `request.resource.data` による入力制約があるか | セキュリティ | 🟡警告 | |
| S-7 | Firestore 書き込み前にバリデーション（Zod 等）が入っているか | セキュリティ | 🟡警告 | |
| K-4 | コミットメッセージに秘密情報が含まれていないか | 秘密情報 | 🟡警告 | |
| B-1 | `async/await` の付け忘れがないか | バグ | 🟡警告 | |
| B-2 | Promise の `.catch()` や `try/catch` でエラーハンドリングされているか | バグ | 🟡警告 | |
| B-3 | `null` / `undefined` チェックがあるか（オプショナルチェーン `?.`） | バグ | 🟡警告 | 【重点】現場ユーザーの想定外操作対策 |
| B-4 | 除算でゼロ除算チェックがあるか | バグ | 🟡警告 | |
| B-5 | 小数点の丸め処理が明示されているか | バグ | 🟡警告 | |
| B-9 | `onSnapshot` のリスナーがアンマウント時に解除されているか | バグ | 🟡警告 | |
| D-1 | タイムゾーン（JST/UTC）が明示的に処理されているか | ドメイン | 🟡警告 | 【重点】物流業務の日付ズレ防止 |

---

## Phase 3：+ 情報 12 項目（2 ヶ月目〜）

| ID | チェック項目 | 観点 | 重大度 |
|----|------------|------|--------|
| B-6 | 単位変換が正しいか（円/ドル、m/mm、day/hour、kg/t） | バグ | 🔵情報 |
| B-7 | 配列・オブジェクトの破壊的操作（`.sort()`, `.splice()`）が意図的か | バグ | 🔵情報 |
| D-2 | 日付のフォーマットが統一されているか（YYYY/MM/DD 等） | ドメイン | 🔵情報 |
| D-3 | CSV 入出力のエンコーディングが一貫しているか（UTF-8/Shift-JIS） | ドメイン | 🔵情報 |
| S-8 | Firebase Storage のアップロードサイズ・ファイル種別が制限されているか | セキュリティ | 🔵情報 |
| M-1 | 1 つのファイルが 300 行を超えていないか | 保守性 | 🔵情報 |
| M-2 | 関数に説明コメントがあるか（特に計算ロジック） | 保守性 | 🔵情報 |
| M-3 | マジックナンバーが使われていないか | 保守性 | 🔵情報 |
| M-4 | console.log がデバッグ用に大量に残っていないか | 保守性 | 🔵情報 |
| C-1 | Cloud Functions 内に無制限ループがないか | コスト | 🔵情報 |
| C-2 | Firestore の全件取得になっていないか（`.limit()` / `.where()`） | コスト | 🔵情報 |
| C-3 | Firebase Storage への大量書き込みが制御されているか | コスト | 🔵情報 |
